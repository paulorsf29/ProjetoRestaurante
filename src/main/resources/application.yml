spring:
  application:
    name: Restaurante
  datasource:
    url: jdbc:postgresql://localhost:5432/restauranteFB_DB
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
        dialect: org.hibernate.dialect.PostgreSQLDialect
        id:
          uuid_representation: BINARY
    open-in-view: false

  logging:
    level:
      org:
        hibernate:
          SQL: DEBUG
          type:
            descriptor:
              sql:
                BasicBinder: TRACE
      com:
        projetoRestaurante: DEBUG